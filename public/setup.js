"use strict";(()=>{var d=document.querySelector("#setup-app"),g=["localhost","127.0.0.1"].includes(location.hostname)&&new URLSearchParams(location.search).get("demo")==="1",f={firebaseAdmin:["Firebase server","Service account for entitlements and admin APIs"],firebaseWeb:["Firebase web login","Public configuration for Google and Apple sign-in"],stripeTest:["Stripe test mode","Test secret, Prices, Coupon and webhook secret"],legacyFulfillment:["Sheets & MailerLite","Legacy key delivery credentials (kept disabled initially)"],adDelivery:["Ad test delivery","Meta and TikTok credentials (not needed for account tests)"],googlePlay:["Google Play sandbox","Developer API and authenticated RTDN"],appleStore:["App Store sandbox","Server API, notification certificates and IDs"],providerTokenEncryption:["Provider token vault","Netlify-only AES-256-GCM key ring for Play subscription recovery"]},p={STRIPE_WEBHOOKS_ENABLED:"Stripe webhooks",GOOGLE_PLAY_WEBHOOKS_ENABLED:"Google Play webhooks",APPLE_WEBHOOKS_ENABLED:"Apple webhooks",OUTBOX_PROCESSING_ENABLED:"Async worker",AD_CONVERSIONS_ENABLED:"Ad conversions",LEGACY_FULFILLMENT_ENABLED:"Legacy fulfillment",SUBSCRIPTION_CANCELLATION_ENABLED:"Subscription cancellation",ACCOUNT_DELETION_PROCESSING_ENABLED:"Account deletion purge",STRIPE_MUTATIONS_ENABLED:"Stripe mutations",APP_CHECK_ENFORCEMENT_ENABLED:"Firebase App Check enforcement",SUBSCRIPTION_RECONCILIATION_ENABLED:"Daily subscription reconciliation"};function n(e){return String(e??"").replace(/[&<>'"]/g,a=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[a])}function l(e){let a=Object.values(e.configuration).filter(Boolean).length,o=Object.keys(e.configuration).length,s=e.status==="ready_for_account_testing";d.innerHTML=`<main class="setup-shell">
    <header class="setup-header"><a class="brand" href="/"><span>W</span><strong>WonderLang</strong></a><nav><a href="/account/">Account</a><a href="/admin/">Operations</a></nav></header>
    <section class="setup-hero"><div><p class="eyebrow">DEPLOYMENT STATUS</p><h1>${s?"Ready for account testing.":"Safe, waiting for test credentials."}</h1><p>This page contains readiness booleans only. It never returns or reads secret values in the browser.</p></div><div class="mode-card ${e.safeMode?"safe":"active"}"><span>${e.safeMode?"SAFE MODE":"TESTING ACTIVE"}</span><strong>${n(e.environment)}</strong><small>${e.safeMode?"All side effects are disabled":"One or more test workflows are enabled"}</small></div></section>
    <section class="progress-panel"><div><span>Configuration readiness</span><strong>${a} / ${o}</strong></div><progress max="${o}" value="${a}"></progress></section>
    <section class="content-grid"><article class="panel"><header><p class="eyebrow">CONNECTIONS</p><h2>Provider readiness</h2></header><div class="status-list">${Object.entries(f).map(([t,[i,c]])=>`<div><i class="${e.configuration[t]?"ready":"waiting"}"></i><span><strong>${i}</strong><small>${c}</small></span><b>${e.configuration[t]?"Ready":"Required"}</b></div>`).join("")}</div></article>
    <article class="panel"><header><p class="eyebrow">KILL SWITCHES</p><h2>Side-effect controls</h2></header><div class="status-list controls">${Object.entries(p).map(([t,i])=>`<div><i class="${e.controls[t]?"enabled":"waiting"}"></i><span><strong>${i}</strong><small>${e.controls[t]?"Enabled intentionally":"Off"}</small></span><b>${e.controls[t]?"On":"Off"}</b></div>`).join("")}</div></article></section>
    <section class="panel next"><header><p class="eyebrow">NEXT GATE</p><h2>${s?"Begin login and checkout tests":"Configure isolated Firebase and Stripe test projects"}</h2></header><ol><li>Connect a separate Firebase test project and enable Google plus Apple.</li><li>Add Stripe <code>sk_test_</code> credentials and test catalog objects.</li><li>Grant the verified test owner an audited Firebase admin claim.</li><li>Enable one test workflow at a time; leave production integrations off.</li></ol><div class="actions"><button id="refresh" type="button">Refresh status</button><a href="/admin/">Open operations console</a></div></section>
    <footer>Deploy ${n(e.deploy||"pending commit metadata")} \xB7 Auto-refreshes every 15 seconds</footer>
  </main>`,document.querySelector("#refresh")?.addEventListener("click",r)}async function r(){if(g){l({status:"configuration_required",environment:"test",safeMode:!0,deploy:"local-preview",configuration:{firebaseAdmin:!1,firebaseWeb:!1,stripeTest:!1,legacyFulfillment:!1,adDelivery:!1,googlePlay:!1,appleStore:!1,providerTokenEncryption:!1},controls:Object.fromEntries(Object.keys(p).map(e=>[e,!1]))});return}try{let e=await fetch("/healthz",{cache:"no-store"});if(!e.ok)throw new Error(`Health request failed (${e.status})`);l(await e.json())}catch(e){d.innerHTML=`<main class="setup-shell"><section class="failure"><h1>Status unavailable</h1><p>${n(e.message)}</p><button id="refresh">Try again</button></section></main>`,document.querySelector("#refresh")?.addEventListener("click",r)}}r();setInterval(r,15e3);})();
