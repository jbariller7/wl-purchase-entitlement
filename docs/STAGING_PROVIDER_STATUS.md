# Staging provider status

Last verified: 2026-08-27. This file records only non-secret identifiers and safety state.

## Safety state

- Netlify site: `wl-purchase-entitlement.netlify.app`
- Firebase test project: `wonderlang-accounts`, owned by `wonderlang.thegame@gmail.com`. The abandoned `wonderlang-entitlements-9590f` project is not used.
- Stripe is test mode only.
- Apple purchases are `Prepare for Submission` / Sandbox configuration.
- All fulfillment, advertising, webhook-processing, cancellation, deletion-processing, outbox, Stripe-mutation, subscription-reconciliation, cloud-storage-monitoring and cloud-save-cleanup switches remain disabled. Only PC/Mac device sign-in and expired-code cleanup are enabled, and only in the Production context of this isolated test site.
- Secret material is stored only in Netlify secret-scoped variables or ignored local build configuration. Never add values to this file.

## Firebase

- Firebase CLI authentication is verified as `wonderlang.thegame@gmail.com`; it can list both the existing analytics project and `wonderlang-accounts`, with `wonderlang-accounts` selected as current. A stale browser callback displayed an OAuth error after the CLI had already retained the valid login, so no retry or alternate account is required.
- On 2026-08-26 project owner `wonderlang.thegame@gmail.com` granted `jonathan@wonderlang.app` the predefined Firebase Admin IAM role for `wonderlang-accounts`. Google Cloud displayed the principal with `Firebase Admin` and confirmed that the policy was updated. The account was not made Owner and the existing owner/service-agent grants were left unchanged.
- Firestore default database: Standard edition, `europe-west9`. The checked-in deny-all client rules compiled and were released on 2026-08-25; a live unauthenticated document-read probe is rejected with HTTP 403. Both checked-in composite indexes are deployed and Firebase reports them Enabled: `legacyKeys(sheetTab, state)` for inventory totals and `entitlements(accessKind, subscriptionState)` for entitlement-state operations.
- Authentication providers enabled: Email/Password and Google. A fresh Identity Toolkit probe returns a valid `google.com` authorization URI for the Netlify account page, and the refreshed Firebase provider table explicitly reports both providers Enabled. Apple remains disabled until its approved credentials are installed. Public authentication identity is `WonderLang`, with `wonderlang.thegame@gmail.com` as the support email.
- Passwordless Email Link is enabled. The live authorized-domain list now contains `wl-purchase-entitlement.netlify.app`, `wonderlang.net`, `www.wonderlang.net`, and the Firebase defaults. Firebase is configured to create separate provider accounts rather than automatically merging accounts that merely present the same email; the customer UI performs only explicit account linking. User sign-up and deletion are enabled, and email-enumeration protection is enabled.
- Admin Settings now contains a protected, read-only Firebase Authentication configuration diagnostic. It reads only the configured account project, authorized domains, passwordless-email policy, account creation/linking/privacy controls and sanitized Google/Apple provider readiness. The provider adapter immediately discards client secrets, Apple private-key contents, API keys, SMTP configuration and raw provider errors. It never changes Firebase Authentication. Three focused tests prove the complete expected configuration, detailed incomplete-state reporting and failure sanitization. Commit `d5cd6d4` published as Netlify deploy `6a8e11793415b20008538a51`; the deployed safe demo rendered all five checks, passed project/domains, passwordless email, account safety and Google, reported only the expected pending Apple provider, kept every processing guard Off and emitted no browser warnings/errors. A real execution still requires the first administrator claim and Apple is expected to remain the only provider failure until a replacement Sign in with Apple key is installed.
- Customer authentication failures are mapped to actionable recovery text for network failure, unavailable or unauthorized providers, canceled popups, disabled accounts, invalid or expired email links, rate limits, blocked browser storage, and provider-link conflicts. Provider collisions explicitly instruct the player to recover the existing account and state that WonderLang never merges accounts automatically; unmapped raw Firebase errors are not rendered.
- The public account configuration initializes Firebase web authentication independently of Stripe. Firebase Admin is now installed in Netlify, so the live response reports `accountApiReady=true` while `checkoutEnabled=false`; authenticated account, entitlement and cloud-save APIs can be tested without enabling payments. Google, email-link, explicit provider linking and sign-out are available for testing. Apple remains unavailable until its provider key is installed.
- Apple provider is pending its private-key configuration.
- Android app: package `com.wonderlang.app`, Firebase app ID `1:1034814537215:android:17850f5b3a3c8b950cbbca`.
- Firebase now contains the public SHA-1 and SHA-256 certificate fingerprints for all three Android signing paths: local debug, WonderLang upload/release, and Google Play App Signing. The upload and Play App Signing SHA-1 values were read directly from Play Console and saved without conflict; their SHA-256 values match the checked-in Digital Asset Links file. Google login can therefore be tested with upload-signed and Play-signed builds. The debug SHA-1 remains registered but Firebase reports that its package/fingerprint pair is owned by an OAuth client in another project. A signed-in Google Cloud inspection confirms the correct-account analytics project `wonderlang-2209c` has no OAuth clients, so debug native-Google testing remains blocked until the abandoned OAuth client is released. No certificate or private signing material was downloaded or copied.
- Android passwordless email uses Firebase Auth's post-Dynamic-Links Hosting flow: the current BoM is `34.15.0`, `WonderLangAccountManager` selects `wonderlang-accounts.firebaseapp.com`, and the manifest intercepts only HTTPS `/__/auth/links` with `android:autoVerify=true`. The source-controlled `firebase-hosting/.well-known/assetlinks.json` covers debug, direct-release/upload and Play App Signing certificates. Firebase Hosting was deployed from the verified `wonderlang.thegame@gmail.com` CLI session on 2026-08-25. Both `wonderlang-accounts.web.app` and `wonderlang-accounts.firebaseapp.com` serve the byte-identical file with HTTP 200, `application/json; charset=utf-8`, the five-minute cache policy and `nosniff`; Google's Digital Asset Links API resolves all three package/certificate associations.
- Blaze is active on `wonderlang-accounts`, linked to the pre-existing direct Google Cloud billing account administered by `wonderlang.thegame@gmail.com`. A EUR 10 monthly billing alert is active; it sends warnings and does not cap spend.
- The default Storage bucket is `wonderlang-accounts.firebasestorage.app` in `europe-west9` (Paris). The checked-in deny-all client rules compiled and were released on 2026-08-25; a corrected live unauthenticated object-list probe is rejected with HTTP 403 `Permission denied`. The intended lifecycle policy deletes only abandoned `cloud-save-profile-uploads/` objects one or more days after creation. The single active CORS configuration exactly matches `storage.cors.json`: the two WonderLang web origins, the isolated Netlify origin and Android `appassets.local`; GET/PUT/HEAD; `Content-Type`, `ETag` and `x-goog-hash`; 3,600 seconds. Public access prevention is enabled and the bucket is not public.
- The Android Firebase client key is injected from `WONDERLANG_ENTITLEMENTS_FIREBASE_API_KEY` in ignored `local.properties` or the Android build environment. It is not tracked in Git.

## Netlify staging deployment

- Verified production UI/application build: `dd8cbac` (Netlify deploy `6a8cfa767423b90008ad1e23`). The published cloud-save retention/cleanup baseline is `580511e` (Netlify deploy `6a8ce2733483ab0008e01166`), the secure NW.js device-sign-in baseline is `4f62c63`, and the earlier honest-UI baseline was `cfd177b`.
- Verified desktop test-build/probe safety build: `10165f6` (Netlify deploy `6a8d0aea7d9f7a0008e45961`). The deploy is ready, serves the updated bridge, remains in test safe mode, reports only 2 of 8 configuration groups ready, and has zero side-effect controls enabled.
- Verified account hidden-state fix: `9919b7c` (Netlify deploy `6a8d0e275f86ba0008386c33`). A live non-demo `/account/` check proves the simulated-demo banner retains its `hidden` attribute, computes to `display: none`, and is absent from the accessibility tree while configuration is incomplete. A separate `?demo=1` check proves the warning remains visible only in explicit demo mode. The regression was caused by the banner's author CSS overriding the browser's native `[hidden]` rule; the component now makes every hidden child authoritative.
- Verified partial-auth build: `c33c7ae` (Netlify deploy `6a8d115dcdef950008d774b8`). The deployed public config reports the Firebase test project and approved USD 6.99/31.99/59.99 display prices while `accountApiReady=false`, `checkoutEnabled=false` and `appCheckEnforced=false`; the real non-demo account page renders Google, Apple and passwordless-email controls instead of the old all-or-nothing setup error. Server-backed account buttons remain disabled. Embedded-browser fallback build `2e41aa9` (Netlify deploy `6a8d12bbbba3560008ca39b6`) retries Google authentication with redirect when popup transport returns `auth/internal-error`. A provider-configuration request made with the runtime web key in memory returned HTTP 200 and a Google authorization URI for the Netlify domain, proving the key/domain/Google-provider configuration is valid. At that build stage the in-app browser still returned `auth/network-request-failed` during the OAuth handoff and created no user; the later verified owner login below supersedes that historical failure. The equivalent Apple check correctly returned `OPERATION_NOT_ALLOWED` because its provider key is intentionally not configured yet.
- Verified authentication-recovery build: `a8f07c6` (Netlify deploy `6a8d14d3e2c5450009c8e72f`). The real account page opened Google's account chooser, selected `jonathan@wonderlang.app`, displayed Google's consent page, and returned to WonderLang. Firebase's redirect-result exchange still failed inside the embedded browser, but the account page rendered the actionable browser guidance instead of a raw Firebase error. Firebase Authentication reported no users at that historical check, proving the failed handoff did not create a partial account; the later verified `jonathan@wonderlang.app` popup login closes this former release gate. `wonderlang.thegame@gmail.com` remains a separate unresolved token-rejection case and is not the administrator account.
- Verified redirect-failure recovery build: `296513d` (Netlify deploy `6a8d16a46ebd9a00083a239f`). A second full embedded Google chooser/consent/return cycle reproduced the environment-specific redirect-result failure, then left Google, Apple and email-link controls visible beside the actionable recovery message instead of forcing a reload. The user list still reported zero users at that historical check, so the repeated failed handoff did not create a partial account.
- Verified Google-helper CSP fix: `343c7ba`. `/account/` and `/admin/` now permit only Firebase's required `https://apis.google.com` helper in addition to same-origin scripts; inline scripts and wildcard script origins remain forbidden. The live response headers contain the exact helper origin.
- Verified account-readiness fix: `9b8e4cc`. Firebase Admin configuration is parsed independently of Stripe, and the live `/api/v1/config` response on 2026-08-25 reports `accountApiReady=true`, `checkoutEnabled=false`, `adminBootstrapEnabled=false`, the `wonderlang-accounts` project and the correct Storage bucket. The live `/healthz` response reports `environment=test`, `safeMode=true`, Firebase Admin/web and legacy Sheets/MailerLite credentials present, with every side-effect control still disabled.
- Verified token-refresh and Firebase Admin recovery code: `1b0c383` forces one fresh Firebase ID token after a server 401 without weakening revocation checks, and `8b06b62` safely normalizes and cryptographically validates multiline, escaped, quoted or flattened service-account PEM values. Firebase Authentication currently lists two Google users, `jonathan@wonderlang.app` and `wonderlang.thegame@gmail.com`, both created and signed in on 2026-08-25. A real `jonathan@wonderlang.app` account request reached the server-authoritative summary successfully. Two fresh `wonderlang.thegame@gmail.com` attempts returned `The Firebase ID token is invalid or revoked`; this remains unresolved and was not repeated after the user's instruction to continue without another test on that address. The audited bootstrap later granted the administrator claim only to `jonathan@wonderlang.app`; the later bootstrap section below is authoritative.
- Audited first-administrator bootstrap: `1b8caba`. It is disabled by default, permits only the exact configured verified Google account after recent authentication and an exact confirmation phrase, preserves unrelated claims, writes an immutable audit record, revokes refresh tokens and is rate limited. It was enabled once for the verified `jonathan@wonderlang.app` account, completed successfully, and was disabled again in every Netlify context.
- Audited administrator cloud-save download: `51cb90b`. The Admin console can request a five-minute signed download only for a manifest-owned immutable revision and writes an audit entry without exposing a permanent or public object URL. The current local response boundary also strips every Storage object path and UID from the customer-detail payload, exposes only validated current/retained revision summaries, and renders the retained-version timeline without offering unaudited historical downloads.
- `APP_ENVIRONMENT=test` in every deploy context.
- Stripe, Google Play and Apple webhook processing; the async worker; ad conversions; legacy fulfillment; subscription cancellation; account-deletion processing; Stripe mutations; scheduled subscription reconciliation; aggregate cloud-storage monitoring; cloud-save revision cleanup; App Check enforcement; and administrator bootstrap remain `false`. On 2026-08-26 only `DEVICE_SIGN_IN_ENABLED` and `DEVICE_SIGN_IN_CLEANUP_ENABLED` were enabled, and only in the Production context of this isolated test site, for the real NW.js device-code test. All other deploy contexts continue to fail closed by default.
- The runtime readiness endpoint historically reported 6 of 8 connection groups configured because it validated that the Google Sheets variable names were present, not that the private key could authenticate. The later protected provider diagnostic below is authoritative: the current Sheets private credential is invalid/undecodable. Firebase Admin, Firebase web configuration, MailerLite, Google Play RTDN routing, provider-token encryption and Stripe test-mode configuration remain present. Published build `df56cc3` reported `ready_for_stripe_canary` and rendered `Account and Stripe configuration are ready. Checkout remains safely off.` It did not claim checkout testing until both `STRIPE_MUTATIONS_ENABLED` and `STRIPE_WEBHOOKS_ENABLED` are intentionally enabled together. Manual Netlify redeploy `6a8e93f2c1d0912437f91585` applied the two device-sign-in controls without enabling payments or processing. `/healthz` consequently reports `safeMode=false` solely because those two isolated login controls are on; checkout testing remains false.
- `PROVIDER_TOKEN_ENCRYPTION_KEYS` now contains a freshly generated AES-256 staging key under non-secret key ID `staging-2026-08-v1`. Netlify masks it and limits it to Builds, Functions and Runtime in Production, Deploy Previews, branch deploys, and Preview Server/Agent Runner contexts; local CLI has no value. No key material was printed, committed or stored in a local file. Reconciliation remains disabled.
- Key inventory alerts currently use the validated default minimum of 10. No `KEY_INVENTORY_LOW_STOCK_THRESHOLDS` per-tab overrides are installed until the actual Steam/Itch replenishment minimums are chosen.
- The protected Google Sheets-versus-Firestore inventory comparison is deployed and its explicit demo path passes. A real administrator execution on 2026-08-27 failed closed with the sanitized `credential_invalid` state: the current Netlify `GOOGLE_PRIVATE_KEY` cannot be decoded. The diagnostic did not return the private key, Sheet ID, access keys, customer data or Google's raw error, and it did not change either Google Sheets or Firestore. Before a dry-run inventory import, the exact protected `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY` and `GOOGLE_SHEET_ID` values must be copied again from `purchased-keys-automation` to `wl-purchase-entitlement`; fulfillment and outbox processing must remain disabled during that repair and comparison.
- Browser smoke testing passed for the `/` redirect, all eight `/admin/` sections, `/account/?demo=1`, `/setup/`, and `/rmmz-test/`. The admin page never silently substitutes demo records when configuration is missing; it displays the real configuration gate. Simulated data requires an explicit `?demo=1` and both customer/admin demos display prominent non-live warnings. Build `6ffa53e` was exercised interactively both locally and from its published Netlify deployment: customer lookup; administrator grant/revoke; account disable/re-enable; session and deletion controls; partial refund with typed confirmation; regional Stripe price replacement while preserving USD and other currencies; CSV import; outbox, cloud-save-cleanup and provider-event retries; alert routing; Google demo sign-in; PC/Mac code preview/approval with removal of `device_code` from the URL; provider-specific subscription management; monthly/Polyglot/Premium checkout gates; and exact-phrase account deletion all produced the intended state transitions. On the published deployment, a two-step EUR Monthly demo change updated the regional row from 6.49 to 7.49 while the USD 6.99 default remained unchanged. Deploy `dd8cbac` adds a durable Premium second-mobile-platform request: the published customer demo submitted and canceled the included iOS request, and the published Admin queue opened and approved it, removed it from the open count, granted permanent iOS access and exposed the immutable audit action without browser warnings/errors. The same build's local safe demo also exercised decline. Fresh 390-pixel checks of the signed-in customer request state and the full Admin customer/decision view have no page-level horizontal overflow; a discovered single-column grid min-content overflow was fixed before deployment. Every rendered customer action/form and every administrator operation family now has an executable client-to-protected-route contract test, including loading, error, empty, permission, disabled, success and explicit-demo states. Admin tables escape all provider/customer strings and permit markup only through an internal symbol-branded cell. Admin Operations renders scheduled-reconciliation history, aggregate token-vault key counts and aggregate Cloud Storage metrics without tokens, player IDs or object paths. Fresh live testing on builds `09db522`, `28fca3c`, `2681b6e` and `95b4038` found and fixed three runtime-only defects: the audited save download called a nonexistent client, the generic customer-demo route intercepted its specific mutation, and a `data:` fetch was blocked by the strict content-security policy. The final published flow downloads the fictional save and records `cloud_save.download` in Admin Audit. The complete customer sandbox now renders real entitlement states and passes Premium request/cancel, Google/Apple/passwordless-email linking, session revocation, account deletion, Premium upgrade confirmation and historical desktop-purchase claiming.
- Published builds `781190e` and `682ffaa` preserve the original Android/iOS platform when an imported historical chapter is upgraded to Polyglot, make legacy chapter products import-only rather than new manual-grant options, and recompute the safe-demo effective-access summary after grant, revoke and Premium second-platform decisions. A fresh live walkthrough executed customer lookup, current-product grant/revoke, Premium price preview/typed commit, historical chapter import preview/typed commit, cleanup/outbox retries, provider-event release, inventory, audit and all-off deployment guards. The customer demo executed Google/Apple entry, passwordless-email linking, Premium cancellation gating, historical desktop-purchase claiming, 30-day deletion confirmation, all-device revocation, Premium request/cancel and PC/Mac device-code approval. The corrected live summary added and then removed iOS permanent access and the Polyglot product exactly with its grant lifecycle.
- Netlify confirms 11 deployed functions. `cloud-save-cleanup` is registered as a scheduled function, while the published health endpoint reports `environment=test`, `safeMode=true`, `status=configuration_required`, and `CLOUD_SAVE_CLEANUP_ENABLED=false`. It was deliberately not manually invoked against the real deployment while disabled-state proof is already covered locally.
- The isolated RPG Maker UI test passed the account panel, cloud-save list, non-destructive save-conflict dialog, and the new PC/Mac code-matching/cancel flow. The live `4f62c63` page renders `ABCD-2345` only as explicitly simulated data, has no browser warnings/errors, and the same UI has no horizontal overflow at 390 pixels.
- A guarded script prepares a disposable desktop RPG Maker MZ test build under the production project's `.codex-tmp` directory, links read-only game assets, isolates saves and NW.js application data, and refuses unmanaged or out-of-scope replacement targets. It leaves the production `js/plugins.js` unchanged. Build `e970e29` strengthened the real NW.js probe so it cannot pass until RPG Maker reaches a scene. The fresh run reached `Scene_Boot`, loaded the account bridge before the cloud plugin, called the deployed staging device-sign-in configuration, received the expected HTTP 503 while the switch is disabled, retained no sign-in token, rendered the visible retryable failure overlay and recorded no fatal or unhandled errors. Four non-fatal base-game sound-load warnings were recorded even though those source files exist; they do not originate in the account plugins. This proves disabled staging fails closed and the plugins load in a real engine scene; it is not yet the real code-to-browser-to-game sign-in test.
- Current local gates: all 278 unit/integration/contract tests pass in the ordinary suite; 15 isolated Firestore/Storage emulator tests remain skipped in that command and passed in their last completed dedicated run; the production TypeScript/widget build passes. New administrator privacy tests prove that operations responses strip outbox payloads/deduplication and lease data, provider event identifiers and payload details, raw Steam/Itch fulfillment keys, Storage paths and UIDs, provider purchase identifiers, and internal deletion workflow fields before the browser sees them. The Admin UI displays validated aggregate/count/timeline data and retains the audited five-minute current-save download workflow. A 2026-08-26 emulator rerun could not start because the managed Java runtime was denied a loopback selector before any rule assertion executed. Direct provider tests cover Google Play Pub/Sub decoding, exact-package rejection, Monthly and Polyglot event routing, no-op connectivity probes, refunds of both original purchases and historical chapter-upgrade grants, and Google/Apple subscription-state normalization. Apple ownership tests prove restored non-consumables retain the original transaction ID, tokenless refund notifications resolve the existing owner, disagreeing account identifiers fail before a grant, and claim ordering uses Apple's signed date. The emulator proves the provider-transaction link and grant write are atomic: a second account cannot claim the same purchase and receives no grant; it also proves millisecond ordering keeps a later refund over a stale receipt replay signed during the same second. Google Play identity tests cover current, linked and expired purchase/account-token ownership, disagreement rejection and attribution-disabled deleted accounts. Provider-mocked integration proves a Play subscriptions-center repurchase is recovered from the prior stored token, persisted under the returning player, acknowledged with the stable WonderLang account token, and deliberately not acknowledged when only a deleted account's pseudonymous audit link remains. The session-revocation suite proves that each PC/Mac approval is bound to a server-side account generation, that a custom token exchanged after `SIGN OUT ALL DEVICES` is rejected when its generation is stale, that a concurrent approval must use Firebase authentication newer than the revocation cutoff, and that account deletion removes `accountSecurity/{uid}`. Earlier production asset, JavaScript syntax, dependency-audit and secret-scan gates remain green and will be rerun before release. Three focused App Links tests prove the exact Android package/domain/path contract, all three SHA-256 signing associations, and hardened JSON response headers. Five focused Premium request tests prove eligibility derivation, privacy-reduced customer responses, idempotent submission/cancellation/resubmission, deterministic approval grants, immutable approval/decline audits, duplicate-decision safety and open-queue removal. The emulator account-deletion test also proves that `secondPlatformRequests/{uid}` and the device-session security record are removed with the rest of the account's personal data. New mixed-ownership tests prove that a monthly subscriber can acquire native permanent access, an already-owned permanent platform cannot be repurchased, and Premium owners are directed to the audited second-platform request instead of being sold another Polyglot purchase. The entitlement API separately reports current mobile access and permanent mobile ownership; subscription totals no longer disappear when the same account also owns a permanent product. The emulator used only `demo-wonderlang-entitlements`; non-emulated service access therefore failed closed. Device-flow tests cover code normalization, account-bound approval, wrong-secret rejection, issuance leasing, abandoned-lease recovery, one-time consumption, secret hashing, bounded revocation/expiry cleanup, generation rotation, stale-token denial and custom-token claim binding. NW.js bridge tests cover runtime-only Firebase configuration, custom-token exchange, project issuer/audience validation, refresh-token persistence and rotation, reload, sign-out deletion, and proof that polling/custom-token values never enter UI events or persistent files. Inventory-policy tests prove validated defaults, exact per-tab overrides, deliberate zero thresholds, malformed-value rejection and typo rejection for unknown Sheet tabs. The storage-monitor suite proves exact-prefix metadata-only inventory, aggregate growth/stale-upload calculations, malformed-size rejection and generic failure persistence without object paths. Cloud-save tests prove that only `save0` through `save20` are accepted, five generations converge to the current plus three predecessors, pruned files are deleted, cross-account or non-revision paths fail closed, provider errors remain sanitized, retries back off, terminal jobs can be audited/requeued, and account deletion removes queued cleanup work. The deployed API rejects an untrusted browser Origin with 403 and no allow-origin response, while the approved WonderLang Origin still reaches the safe configuration gate. Authenticated account and administrator APIs have atomic per-account/action limits backed by hashed Firestore keys; failures reject protected requests. Firebase App Check verification is present but its enforcement switch remains false until all clients are provisioned. The reconciliation suite proves authenticated encryption and key rotation, lease exclusion, provider-failure isolation, daily scheduling, revoked-subscription shutdown, and deletion-race resistance; a late provider response cannot recreate an erased Play token or re-enable a deleted account's retained subscription link. The emulator suite also proves that provider inboxes retain only payload digests, completed outbox payloads are redacted, and final account deletion pseudonymizes linked orders/ledger rows while canceling unfinished personal side effects. Provider-mocked tests prove that final deletion clears only Google Sheet email cells and issues MailerLite GDPR forget requests; a live external deletion canary is still required before enabling deletion processing.

- A fresh production dependency audit reports zero vulnerabilities. The tracked-source credential scan finds no Stripe, webhook, cloud or private-key material; the only PEM markers are parser literals. These gates will still be rerun before release.
- Verified Apple-notification application tests prove that a verified Monthly event creates the expected subscription ledger row and that an eligible historical chapter purchase creates both its original record and the promised permanent Polyglot upgrade. Non-consumables now use `originalTransactionId` as the canonical ledger key across purchase and restore transactions, retain the latest transaction ID only as audit metadata, route tokenless refunds through the canonical owner link, reject account-token/ledger-owner disagreement, and reject claims without Apple's signed chronology so a stale pre-refund receipt cannot be replayed as a newer event.
- Direct Stripe event-processor coverage proves paid-versus-unpaid Premium grants, exactly one selected PC/Mac fulfillment route, historical website fulfillment, expired discount release, retained historical Monthly synchronization and grace, full-versus-partial refund policy, and dispute revocation without contacting Stripe.
- Fulfillment replay coverage raises the current ordinary suite to 209 passing tests. Reprocessing the same Checkout Session reuses the same transactional key allocation, rewrites only the same Google Sheet cells, and upserts the same MailerLite subscriber fields/groups. A MailerLite failure after allocation reuses the original key on retry, and insufficient inventory fails before any Sheet or MailerLite call.
- Advertising-delivery coverage raises the current ordinary suite to 212 passing tests. Stripe checkout processing queues one Meta and one TikTok conversion with stable provider-specific deduplication keys, a shared event ID, normalized value/currency, and only hashed email/account identifiers. Direct sender tests prove the expected click identifiers and test-event codes are preserved, raw email is absent, and provider response bodies cannot leak through operational errors. Advertising delivery remains disabled.
- Provider-isolation build `598aabd` adds two configuration regression tests; the ordinary suite now passes 180 tests. The published `/healthz` response remains in test safe mode with account testing ready, checkout testing unavailable, Google Play ready, and every side-effect control disabled. The approved-origin CORS preflight returns a bodyless `204` (`0` downloaded bytes).
- Strict health readiness now invokes the same validated Firebase, Stripe, Google Play, Apple and token-encryption parsers used by runtime code. Missing checkout return URLs or a required production Apple app ID can no longer produce a false provider-ready signal. The ordinary suite now passes 181 tests.
- Purchase-channel hardening removes Monthly and Polyglot Stripe controls from the website, rejects crafted native-product checkout requests before Stripe access, and limits Admin Stripe price mutations to Premium. Native prices remain visible as Play/App Store reference data, while historical Stripe Monthly/Polyglot event and refund support is retained. The ordinary suite now passes 184 tests.
- Published purchase-channel build `ce8cd4b` (Netlify deploy `6a8db854ffddc20008f39d0b`) was exercised in the live browser after a first pass exposed and fixed a removed-button reference that had left Account initialization at `Loading account…`. The corrected Account demo signs in, renders Monthly and Polyglot as native-app-store-only offers with no purchase controls, renders the Premium platform/delivery selectors, blocks Premium checkout until subscription-cancellation confirmation, then validates the side-effect-free demo checkout. All eight Admin sections render their expected forms, tables and controls; the Premium-only price preview produces the exact typed confirmation while Monthly and Polyglot remain labeled as Play/App Store-managed. No browser errors were emitted after the corrected deploy. Live health remains `ready_for_account_testing`, `test`, safe mode, Google Play ready, Stripe/Apple unavailable and zero side-effect controls enabled.
- Published functional-UI build `9a705f6` (Netlify deploy `6a8dd4c575f0370008e48171`) was exercised again in the live isolated demos. Admin Premium second-platform approval created the expected permanent iOS grant and removed the request from the open queue; Premium price preview/typed commit, CSV import preview/typed commit, failed cleanup retry, failed outbox retry, provider-event release, key inventory, audit propagation and all-off deployment guards all behaved correctly. The customer demo enforced the subscription-cancellation checkbox before Premium checkout, preserved the chosen iOS/direct-download options, validated Premium checkout without opening payment, exercised the Stripe portal, mobile restore and email-linking controls, and now renders `Google, Apple, Email` instead of Firebase's internal `password` provider ID. Neither live demo emitted a browser warning or error, and no external service or real record was mutated.
- The deterministic Android storefront audit currently passes 26 of 27 checks. Its remaining rule requires all four chapter products to remain visibly purchasable, which conflicts with the approved policy that chapter SKUs are restore-only and new sales are limited to Monthly and Polyglot. Chapter sales were not reintroduced and the audit was not weakened. A 2026-08-27 commercial-rule audit found and fixed two separate storefront problems: Polyglot ownership no longer suppresses the Monthly offer, so a permanent owner can still add cloud saves by subscribing; and the `3 DAYS FREE` ribbon/CTA is now driven by the exact eligible Play offer, so a trial-ineligible account is not promised a free trial. The authoritative Android, editable Project1 and maintained mirror storefront files are byte-identical after the fixes; all copies pass JavaScript syntax, all 27 focused integration tests pass, and the complete ordinary suite remains 293/293. The native release still requires the real-device tap-to-Billing matrix.

## Stripe test catalog

- New website checkout is Premium-only. The Monthly and Polyglot Stripe test objects below are retained for historical event/refund/migration coverage and are not customer-facing checkout options.
- Mobile Monthly product: `prod_V88ycRQwGJwJXL`
- Mobile Monthly replacement price: `price_1U80wvBFbQoDa6p0gyuJ7ibY`, USD 6.99/month plus the approved regional prices
- Polyglot Permanent product: `prod_V8HMfV6ZjgSsYA`
- Polyglot Permanent price: `price_1U80o0BFbQoDa6p0b8u97nPq`, USD 31.99 plus the approved regional prices
- Premium Lifetime product: `prod_V8B2TQgSmAHShK`
- Premium Lifetime replacement price: `price_1U80jyBFbQoDa6p0RI72ckxu`, USD 59.99 plus the approved regional prices
- The pre-split lifetime price `price_1U7ug9BFbQoDa6p0qrHmdEdq` and original monthly price `price_1U7sgkBFbQoDa6p0nb1vGaSy` remain available for transaction history and existing customers.
- Stripe offered 36 of the 37 requested currencies. KWD was unavailable in the price selector and was not substituted. JPY Monthly is stored as JPY 787 because Stripe treats JPY as zero-decimal.
- Historical-owner coupon: `wonderlang_desktop_owner_lifetime_50`
- Billing Portal is configured.
- Admin Settings now contains a protected, read-only Stripe catalog diagnostic. It retrieves the configured Monthly history Price, Polyglot history Price, Premium checkout Price, each Product, all expected regional currency amounts (with KWD explicitly optional because Stripe rejected it), and the historical-owner 50% once-only Coupon. It returns only non-secret catalog facts, sanitizes provider failures, never creates or changes a Stripe object, and reports the two canary switches separately. Two focused tests prove both the passing restricted-key path and sanitized mismatch/failure behavior. A real execution still requires the first administrator claim.
- Restricted test key `WonderLang staging backend` is created and installed in Netlify as the secret-scoped `STRIPE_SECRET_KEY`. Its exact permissions are Customers `Write`; Checkout Sessions `Write`; Customer Portal `Write`; Prices `Write`; Products `Read`; Coupons `Read`; Subscriptions `Write`; Payment Intents `Read`; Charges and Refunds `Write`; every unrelated Stripe resource `None`. It is scoped by Netlify to Builds, Functions and Runtime in all four deploy contexts. The credential value was never printed, committed or written to a local file.
- Stripe test webhook destination `we_1U8QaIBFbQoDa6p0OV04QkxI`, named `WonderLang staging entitlements`, posts Snapshot events to `https://wl-purchase-entitlement.netlify.app/webhooks/stripe` using API version `2023-10-16`. Its 12 selected events are `checkout.session.completed`, `checkout.session.async_payment_succeeded`, `checkout.session.expired`, `invoice.paid`, `invoice.payment_failed`, `customer.subscription.created`, `customer.subscription.updated`, `customer.subscription.deleted`, `customer.subscription.paused`, `customer.subscription.resumed`, `charge.refunded`, and `charge.dispute.created`. Its signing secret is installed in Netlify as secret-scoped `STRIPE_WEBHOOK_SECRET` with the same Builds/Functions/Runtime and four-context coverage; the value was never printed, committed or written to a local file.
- `STRIPE_WEBHOOKS_ENABLED=false` and `STRIPE_MUTATIONS_ENABLED=false` remain authoritative in every deploy context. Creating the endpoint and installing credentials does not process events or permit application-initiated Stripe mutations. A controlled test Checkout/Portal/webhook/refund/dispute canary is still required before either switch is changed. The previous standard test secret remains outside Netlify and must be rotated after the restricted-key canary; rotation is deliberately deferred because it is destructive and may affect other test tooling.
- A fresh signed-in browser check confirmed that Mobile Monthly is active in Stripe's sandbox, retains the original USD 6.99/month Price for history, and exposes `price_1U80wvBFbQoDa6p0gyuJ7ibY` as the separate "approved regional prices" Price. The same test-mode inspection confirmed Premium product `prod_V8B2TQgSmAHShK` retains the old default USD 60.00 Price `price_1U7ug9BFbQoDa6p0qrHmdEdq` for history and has the separate USD 59.99 regional Price `price_1U80jyBFbQoDa6p0RI72ckxu`. No live-mode Stripe object was changed.

## Google Play

- Developer organization ID: `6814081401818264265`
- App: `com.wonderlang.app`, Play app ID `4972386637208238631`
- Monthly subscription product: `wonderlangmonthly`, displayed as Mobile Monthly with full-game, cloud-save and Android/iOS benefits
- Unsaved base-plan draft: `monthly`, monthly renewal, USD 6.99, 177 regions. Google computed regional prices, but its automatic conversions do not reproduce the approved regional-discount table.
- Base-plan save returns `Your changes couldn't be saved`; a fresh authorized retry on 2026-08-24 returned the same provider-side failure. The known account prerequisite is unresolved Taiwan and Ireland payments-profile tax information.
- The three-day introductory offer can be created only after the base plan saves.
- `wonderlangfull` is displayed as Polyglot Permanent Access with a no-cloud-save description. Its legacy-compatible `buy` purchase option remains active at its original United States price of USD 25.99 for the currently released app.
- Promo-code campaign `PR Code` (`120231895`) is paused. Its two historical redemptions remain recorded; additional redemptions are disabled.
- A second, non-legacy purchase option is saved as a draft: `buy-polyglot-permanent`, USD 31.99 base with Google-managed localized equivalents across 173 countries/regions. It has not been activated. The Android Billing 9 build explicitly selects this option by `purchaseOptionId` and will not silently fall back to `buy`.
- A fresh signed-in owner-account browser check on 2026-08-27 confirmed the intended live/draft split: `buy` remains Active, `buy-polyglot-permanent` remains Draft, and the latter has not been activated ahead of the matching app update. It also confirmed that `wonderlangmonthly` exists but still has zero active base plans while Play Console displays the payments-profile warning.
- Admin Settings now also contains a protected, read-only Google Play catalog diagnostic. It reads `wonderlangmonthly`, expected base plan `monthly`, free three-day offers, `wonderlangfull`, legacy purchase option `buy`, and `buy-polyglot-permanent`. In the default pre-update rollout it requires Monthly to be active at USD 6.99 with an active free `P3D` offer, legacy `buy` to remain Active and Billing-compatible at USD 25.99, and the new option to remain Draft at USD 31.99. The explicit `GOOGLE_PLAY_POLYGLOT_ROLLOUT_PHASE` changes only the new option's expected state when the compatible Android update is ready. It never acknowledges a purchase or mutates Play Console, returns no service-account details, and sanitizes provider failures. Three focused tests cover the pre-update split, compatible-update transition and missing/error path. Commit `a572e7f` published as Netlify deploy `6a8e0a579c9dd20008106f4f`; the deployed safe demo rendered all four expected checks, states and USD prices, reported Play webhook processing Off, completed the action with a success notice, and produced no browser warnings/errors. A real execution still requires the first administrator claim and is expected to report the known unsaved Monthly base plan/trial until Play Console tax prerequisites are resolved.
- Google Play previously reused the key-fulfillment service account through `GOOGLE_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PRIVATE_KEY`. The protected catalog diagnostic later proved that the deployed private key was malformed for Android Publisher use. Current source now requires an independent Netlify-only `GOOGLE_PLAY_SERVICE_ACCOUNT_EMAIL` / `GOOGLE_PLAY_PRIVATE_KEY` credential for verification and acknowledgement; the original variables remain dedicated to Google Sheets fulfillment. The new Play credential still has to be created/authorized and installed before the Play diagnostic can pass.
- A read-only owner-account audit found that the historical `netlify-keys-bot` is active in Play Console with administrator access to both the suspended legacy package and `com.wonderlang.app`, while `wonderlang.thegame@gmail.com` cannot manage the bot's separate Google Cloud project. The replacement therefore belongs in `wonderlang-accounts`, is granted access only to `com.wonderlang.app`, and receives only View app information, View financial data and Manage orders and subscriptions. It receives no release, tester, policy, review, store-presence or user-management permission.
- Google Cloud topic `projects/wonderlang-accounts/topics/wonderlang-google-play-rtdn` grants only Pub/Sub Publisher to Google Play. Active subscription `wonderlang-google-play-rtdn-netlify` sends wrapped messages to the exact Netlify `/webhooks/google-play` endpoint, uses exponential retry, never expires, and authenticates with audience equal to that endpoint. Its keyless identity is `wonderlang-rtdn-push@wonderlang-accounts.iam.gserviceaccount.com`; the Google-managed Pub/Sub service agent received the standard Service Account Token Creator role so it can mint short-lived OIDC tokens. No private key exists for the push identity.
- Play Console real-time developer notifications are saved for subscriptions, voided purchases and all one-time products. The matching package, audience and push-identity email are installed in Netlify for all deploy contexts. `GOOGLE_PLAY_WEBHOOKS_ENABLED` remains `false`. A live provider test traversed Google Play → Pub/Sub → OIDC-authenticated Netlify and was acknowledged exactly once without opening Firestore or Android Publisher. The first attempt exposed Netlify's incompatibility with a body-bearing 204; build `461807e` removed bodies from every 204 response, the retained Pub/Sub retry then completed cleanly, and no further retry appeared.
- Google Play, Apple, Stripe, Firebase Admin, provider-token encryption, Meta delivery, TikTok delivery and cloud-storage monitoring now parse through independent validated configuration boundaries. An unfinished provider cannot prevent an unrelated notification, receipt check, monitoring run or ad-delivery diagnostic. While Play processing is disabled, the webhook accepts only a correctly signed Google `testNotification` connectivity probe and never opens Firestore or calls Android Publisher; signed real events still return 503 for retry, and missing/invalid signatures fail closed.
- The immutable historical `PR Code` campaign still references `wonderlangfull` because two codes were previously redeemed, but its current status is Paused and the only available action is `Resume promotion`; no additional redemptions are enabled.
- `wonderlangch1` through `wonderlangch4` remain restorable; chapter offers remain hidden from new-sale UI.
- The authoritative Android project matches the tested mirror for Billing, `MainActivity`, `WonderLangAccountManager`, manifest, Firebase resource metadata, plugin registration and build configuration. The abandoned Firebase domain and project metadata were removed from the Android copy; its isolated named app, email-link domain, app ID, project ID, bucket, sender ID and Google web client ID now select `wonderlang-accounts`. The existing default `wonderlang-2209c` Firebase app remains dedicated to Analytics and Crashlytics. The Android client key was transferred from Netlify into ignored `local.properties` without printing or committing it. Both Android/editable storefront files and both account/cloud-save files pass JavaScript syntax checks. Every maintained integration file matches its deployed Android copy byte-for-byte; the only full-project comparison difference is a harmless trailing blank line in `gradle/libs.versions.toml`. The latest account manager adds an Android-Keystore-encrypted, Firebase-UID-bound offline entitlement lease: Monthly is limited to seven days and the known paid/grace deadline, permanent/Premium revalidates every 30 days, WebView storage is not authoritative, and lease expiry removes access without hiding the signed-in identity.
- The last complete `:app:assembleDebug` succeeded before the offline-lease source change. That earlier 115,556,169-byte APK has SHA-256 `EACB08356401003730A79B44C42D7F530B66DF626623CEBB62559E852B3FD5F3`; its packaged manifest contains package `com.wonderlang.app`, HTTPS `autoVerify`, host `wonderlang-accounts.firebaseapp.com` and `/__/auth/links`, while its packaged resources contain the correct account project and bucket and no abandoned-project identifier. After the current source change, repeated `:app:compileDebugKotlin --rerun-tasks` attempts—including both available selector providers—failed before Kotlin compilation because the managed Java runtime could not create Gradle's local selector/loopback connection (`PipeImpl`, `WEPollSelectorImpl` / `WindowsSelectorImpl`). Compilation of the new Kotlin hash is therefore still unproven; this is recorded as an environment gate, not treated as a passing or failing source compile.

## App Check staging

- The 2026-08-27 ordinary TypeScript/unit/integration/contract run passes 293 tests; the dedicated rules suite contains 15 isolated Firestore/Storage cases. Those rules cases were skipped in this run because the managed Java host cannot create the required loopback selector.
- Web account and administrator clients optionally initialize reCAPTCHA Enterprise and attach `X-Firebase-AppCheck`. Android installs the Play Integrity provider on the named `wonderlang-accounts` Firebase app and attaches a best-effort token.
- The web CSP contains Google's documented reCAPTCHA script, frame and connection sources. `APP_CHECK_ENFORCEMENT_ENABLED` remains `false` until the web and Android providers are registered, metrics are monitored, the actual iOS client is integrated, and production-equivalent device tests pass.
- Commit `914c85f` deployed as Netlify deploy `6a8de0e73450a500087439c7`. Live probes returned HTTP 200 for both account and administrator pages, found the App Check header plumbing in both production bundles, found every required reCAPTCHA CSP source, and confirmed `environment=test`, `accountApiReady=true`, `checkoutEnabled=false`, `appCheckConfigured=false`, and `appCheckEnforced=false` without printing public client identifiers.

## Apple

- Team ID: `8L2M38663F`
- App Store app ID: `6780447024`
- Bundle ID: `com.wonderlang.app`
- Sign in with Apple capability is enabled. Provisioning profiles containing this App ID must be regenerated before the next iOS build.
- Services ID: `com.wonderlang.account`
- Required web sign-in domain: `wonderlang-accounts.firebaseapp.com`
- Required return URL: `https://wonderlang-accounts.firebaseapp.com/__/auth/handler`
- Apple Developer now registers the correct `wonderlang-accounts.firebaseapp.com` domain and Firebase callback under `com.wonderlang.account`, grouped with primary App ID `8L2M38663F.com.wonderlang.app`. The abandoned-project domain/return URL remains registered temporarily for transition safety; it is not used by the current application.
- Subscription group: `WonderLang Premium`, ID `22331966`
- Monthly product: `wonderlangmonthly`, displayed as Mobile Monthly, Apple ID `6804702003`, one month, USD 6.99 base, all 175 regions
- Introductory offer: free for the first three days, starts 2026-08-24, no end date, all 175 regions
- Polyglot Permanent non-consumable: existing `wonderlangfull`, displayed as Polyglot Permanent Access, Apple ID `6789931887`; its current base price is USD 31.99 across 175 regions with Apple-managed comparable tiers.
- Apple storefront tiers do not exactly reproduce the website/Stripe regional table; the USD 31.99 global price schedule is the saved provider-authoritative catalog.
- Existing `wonderlangch1` through `wonderlangch4` products remain present for restore and server-side migration.
- Admin Settings contains a protected, read-only App Store Connect catalog diagnostic for app/bundle identity, Monthly's subscription group, one-month period and USD 6.99 price, the current free three-day trial, Polyglot's non-consumable type and USD 31.99 price, and all four restore-only chapter products. It creates an ES256 JWT valid for two minutes, sends only exact GET requests, never returns the token, key ID, issuer or private key, and sanitizes provider failures. Removed-from-sale historical chapters remain valid for restore and are not required to be resold. Four focused diagnostic tests plus an isolated configuration-boundary test pass. Commit `cbab7e6` published as Netlify deploy `6a8e175a7669840007a9b012`; the deployed safe demo rendered and passed all five catalog checks, displayed the expected USD prices and product types, retained all 16 processing guards Off and emitted no browser warnings/errors. Real execution remains gated on the separate App Store Connect Server API key.
- Two unusable Sign in with Apple keys had one-time `.p8` downloads that the in-app browser did not expose: `WonderLang Firebase Sign In` (`3K54H65AV8`) and `WonderLang Firebase Sign In Replacement` (`7WYANX7RHL`). After exact-ID verification and explicit user approval, both were revoked on 2026-08-25; neither remains in the active Apple Developer key list. Creating one clean replacement still requires fresh action-time confirmation, and its one-time `.p8` download must be saved through a normal local browser. Firebase's Apple provider form is prepared with Services ID `com.wonderlang.account` and the correct callback, but no provider change has been saved. App Store Server API and Notifications V2 credentials remain separate pending work.

## Completed credential-security remediation

GitHub secret-scanning alert `#1` was traced to the Firebase Android client key in commit `641a64b`, belonging to the abandoned `wonderlang-entitlements-9590f` project. Public `main` history was rewritten without the value and the current tree has regression coverage. After authenticating the old project owner, Google Cloud showed exactly two Firebase-created API credentials. The exposed Android key resource `7fa7bb10-8320-4cfe-b844-fc7b243fb310` was verified in the abandoned project and deleted on 2026-08-26; a fresh credential-list read proved the Android key absent and left the separate old-project Browser key intact. No `wonderlang-accounts` credential was changed. GitHub alert `#1` is now resolved with resolution `revoked`, and Netlify's active `FIREBASE_WEB_API_KEY` remains secret-scoped.

## Firebase administrator bootstrap and account availability

- Public `/api/v1/config` no longer performs a Stripe API request. It reads the cached Firestore catalog when available and falls back to the approved USD/regional catalog when Firestore is unavailable. A regression test proves that an invalid/unavailable Stripe provider cannot take down Firebase account login. Commit `6c90e55` published successfully and restored the real Account page after the prior restricted-key failure.
- The one-time audited bootstrap granted the Firebase `admin` custom claim only to the verified `jonathan@wonderlang.app` account. The operation created its audit record, revoked existing sessions and redirected to the protected Operations sign-in as designed.
- `ADMIN_BOOTSTRAP_ENABLED` was immediately changed back to `false` in every Netlify deploy context and a clean production redeploy published that disabled setting. Purchasing, provider webhooks, outbox processing, fulfillment, advertising conversions, cancellation processing, deletion processing and App Check enforcement remain disabled.
- The ordinary suite passes 255 tests, the dedicated Firebase emulator suite passes all 15 deny-by-default and server-invariant tests, the production build passes, `npm audit --omit=dev` reports zero vulnerabilities, and the repository scan finds no Firebase, Stripe or webhook secret values. The only private-key marker is the expected parser in `src/infrastructure/private-key.ts`.
- Real testing rejected an attempted Firebase redirect-login compatibility path because the hosted handler returned `The requested action is invalid` in the embedded browser. That path was removed rather than left as an unverified fallback. The verified Google popup flow remains authoritative; it completed the real administrator bootstrap successfully.
- A fresh real Google administrator session then loaded all eight protected Operations sections with the server-verified claim and all 16 deployment guards Off. Mobile 390×844 testing rendered the customer sign-in UI and every administrator section without horizontal layout loss; the temporary viewport was reset afterward.
- Real diagnostics exposed three configuration/code issues. Catalog reads still contacted Stripe, the installed restricted Stripe key is rejected by Stripe, and the installed Google Play private key does not parse as a service-account PEM. Catalog reads are now provider-independent, the Firebase diagnostic accepts Identity Platform's canonical numeric project resource name, and malformed Google Play credentials return sanitized failed checks instead of a generic server error. Replacing the two invalid Netlify provider credentials remains an operator step.
- Audit History now merges the immutable `adminBootstrapAudit` record into the ordinary administrator history response, so the initial claim grant is visible with its actor, target, state and Google provider while the original security record remains untouched.
- The Android/RPG Maker release-safety rerun on 2026-08-26 passed JavaScript syntax for the editable and packaged storefront, cloud plugin, desktop bridge and disposable runtime probe. The ordinary suite now passes 256 tests, the production build passes and `npm audit --omit=dev` reports zero vulnerabilities. The deterministic Android storefront audit passed 26 of 27 checks; its only failure is the known obsolete requirement to resell the four restore-only chapter products. A fresh Kotlin/Gradle and Firebase-emulator rerun could not start because the current managed execution environment rejects Java loopback selectors before project or rule evaluation (`Unable to establish loopback connection`). No Kotlin compile error or rule assertion failure occurred; the prior unchanged-hash APK and prior 15/15 emulator result remain the last completed Java gates.
- After the 2026-08-27 Monthly ownership/trial fixes, the complete ordinary suite passes 293 tests and TypeScript compilation passes. The current storefront files remain byte-identical and syntax-clean. A fresh authoritative `:app:compileDebugKotlin --no-daemon --rerun-tasks` attempt again stopped at `Unable to establish loopback connection` before Kotlin evaluation; the newly changed Kotlin hash therefore still needs compilation in Android Studio or another host before an internal-track build is uploaded.
- A later Android account-switch audit bound both the cached Firebase ID token and cached Google Play obfuscated account token to the exact current Firebase UID and immediately clears either cache when the Auth listener observes a different user. A stale refresh from account A can no longer supply either token after account B signs in, including the offline-lease fallback path. The maintained mirror and authoritative Android manager are byte-identical at SHA-256 `A63116CF8C41C56B91A224B06562575969DD7F766E2A4D130645AA2F31A75A18`; 45 focused Android/backend/profile tests and TypeScript compilation pass. Direct Android Publisher mocks now cover active Polyglot acknowledgment, pending non-grant behavior, historical chapter migration and receipt/SKU mismatch rejection. Gradle still stops before Kotlin evaluation at the managed host's loopback connection failure, so this new Kotlin hash remains a real build/device gate.
- Google Play's current Billing 9.1 and SubscriptionPurchaseV2 documentation was rechecked before freezing the Android lifecycle mapping: active/canceled-before-expiry and grace retain access; account hold, pause, expiry and revocation do not. Android now refreshes the server entitlement snapshot as well as INAPP/SUBS purchases on every signed-in `onResume`, so an RTDN/reconciliation change cannot wait indefinitely behind an older local lease. The authoritative and mirrored `MainActivity.kt` are byte-identical at SHA-256 `F436FE3860AF20C31D066DCE6E5B4BB3F619BC56E81A0CCDE829CE11AD2AFA20`; 53 focused lifecycle/store tests pass. The deterministic storefront audit remains 26/27 only because its final rule contradicts the approved restore-only chapter policy.
- The provider mapper now also requires a valid future `expiryTime` before any Play `ACTIVE`, `IN_GRACE_PERIOD` or not-yet-expired `CANCELED` state can grant access. A missing, malformed or elapsed provider deadline fails closed instead of creating an open-ended subscription grant; `PENDING` remains non-entitled and pending. Focused lifecycle coverage passes all 27 cases.
- A 2026-08-27 profile-restore audit fixed a missing `applyingProfile` guard around `StorageManager.saveObject`: downloading a profile no longer schedules an immediate redundant upload/revision while the downloaded files are being written. The Android packaged copy, entitlement-repository mirror, public RMMZ test copy, canonical RMMZ integration and editable Project1 plugin are now byte-identical and syntax-clean; 31 focused Android/profile contract tests pass.
- Netlify published the guarded profile plugin from commit `83aeaf7`. A fresh deployed `/rmmz-test/` walkthrough opened the complete-profile manager, rendered the Default profile and all profile actions, opened the two-device conflict dialog, chose `Use cloud profile`, and remained closed after the upload debounce window instead of reopening a redundant conflict/upload flow. The harness is isolated and touches no real save or provider.
- The Android whole-profile client now carries the named Firebase app's auto-refreshed Play Integrity App Check token across the trusted local `WLAccountManager` bridge and attaches it to every WebView cloud request. Native calls also refresh the shared cache whenever they obtain a token. The canonical integration, Android packaged mirror, public harness, editable Project1 plugin and authoritative Android plugin are byte-identical at SHA-256 `5CA268EC8A01C802C57A5021510D6F6B8B84AE2498576D33657ACA294B16BB1C`; the mirrored and authoritative account managers are byte-identical at `D04A30C08E83E3FA1D63C42F56800A4747D4E5DE9A94E1CC25312061A1C0A2C4`. All five JavaScript copies pass syntax checks, 302 ordinary tests and TypeScript compilation pass, and the storefront audit remains 26/27 solely because its historical chapter-sale rule conflicts with the approved restore-only chapter policy. A fresh Gradle attempt again stopped at the managed host's loopback restriction before Kotlin evaluation.
- PC/Mac staging device sign-in now passes the safe live pre-approval gates. A fresh disposable RPG Maker MZ/NW.js build loaded `WonderLangDesktopAccountBridge` and `WonderLangAccountCloudSync` in the real engine, selected the correct Netlify API in both plugins, reached `Scene_Boot`, and produced no fatal or console problems. The probe now records only the expected project identifier and an API-key-presence boolean, never the Firebase client-key value. Its real start/poll canary returned HTTP 201 then 202, proved the formatted eight-character code, separate 43-character polling secret, valid account URL, `pending` state and absence of any Firebase custom token before account approval, and persisted neither short-lived value in its report. The Admin Operations response and UI now add aggregate pending/approved/issuing/consumed/expired device-session counts while excluding codes, polling secrets, device labels and player identities. Real signed-in approval, one-time token exchange, persisted refresh, revocation and two-installation cloud-save conflict remain pending because the short-lived approval code must be entered with the user's action-time confirmation.
- The current local customer and administrator assets were exercised again in a real browser after the aggregate device-session monitor was added. All eight Admin sections rendered; customer lookup, audited entitlement grant plus revoke confirmation, Premium price preview, CSV dry run and a failed-cleanup retry reached the expected states. The customer demo completed Google, Apple and passwordless-email entry, mobile purchase restoration, Stripe portal simulation and the guarded Premium checkout flow. The new Operations table displayed only aggregate pending/approved/issuing/consumed/expired counts. A later local privacy pass rendered a current cloud save plus two retained revisions, opened the revision-history disclosure, and confirmed that the Admin document has no horizontal overflow at 390×844 and emits no console warnings/errors. Both portals remained free of page-level horizontal overflow; the Admin mobile menu successfully navigated to Settings. This verifies the local build only; publication still requires an approved Git push and Netlify deploy.

## Unverified release gates

- Real Apple and passwordless-email sign-in against Firebase test credentials
- Stripe test Checkout, Portal and webhook replay
- Play license-tester purchase and backend verification
- Apple Sandbox purchase and App Store Server Notification verification
- Two-installation cloud-save upload/restore/conflict test
- Real provider-backed administrator diagnostics and controlled canaries against the configured test services; all UI actions are verified in the side-effect-free demo, and the real administrator claim/protected-route gate is verified
- Real-device Android tap, cancel, scroll and rapid-double-tap Billing matrix
- Installed-device verification that a real Firebase passwordless email link opens `com.wonderlang.app` directly and completes sign-in for both Play-signed and test builds
- Real signed-in PC/Mac device-code approval, one-time Firebase token exchange, refresh persistence, session revocation and sign-out deletion
- The deterministic storefront audit still contains a legacy rule requiring all four chapter offers to remain on sale. This conflicts with the approved commercial model in which chapter SKUs are restore-only; do not satisfy that rule by reintroducing chapter sales.

Code complete; real-device Android tap-to-Billing verification is still required before release.
